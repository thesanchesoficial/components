import 'package:flutter/material.dart';

class OwTextFieldStf extends StatefulWidget {
  @override
  _OwTextFieldStfState createState() => _OwTextFieldStfState();
}

class _OwTextFieldStfState extends State<OwTextFieldStf> { // ! Make the OwTextField with the FieldType that needs a stateful widget (like: password)
  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}