import 'package:flutter/material.dart';

class OwShowImage extends StatelessWidget { // ! Finalizar
  // final String url;
  // final double height;
  // final double width;
  // final BoxFit fit;
  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}